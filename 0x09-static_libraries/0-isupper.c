#include "main.h"
/**
 * _isupper - returns 1 if the c is a uppercase otherwise 0
 * @chara: the char to be entered
 * Return: O mean lowercase
 */
int _isupper(int chara)
{
	return ((chara >= 65) && (chara <= 90));
}
