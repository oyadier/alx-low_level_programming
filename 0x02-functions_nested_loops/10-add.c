#include "main.h"
/**
 * add - adds two number and return result
 * @_first: first number to be added
 * @_second: second number to be added
 * Return: returns the result
 * */
int add(int _first, int _second)
{
	return (_first + _second);
}
