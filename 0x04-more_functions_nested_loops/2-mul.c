#include "main.h"
/**
 * mul - this multiply two integers
 * @_inte1: first integer to be entered
 * @_inte2: second integer to be entered
 * Return: the result of the multiplied number
 */
int mul(int _inte1, int _inte2)
{
	return (_inte1 * _inte2);
}
