#include "main.h"
/**
 * _isdigit - return 1 if it's digit else 0
 * @chara: the character to be entered
 * Return: 1 means is digit
 */
int _isdigit(int chara)
{
	return ((chara >= 48) && (chara <= 57));
}
