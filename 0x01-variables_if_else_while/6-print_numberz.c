#include <stdio.h>
/**
 * main - contains single digit numbers
 * Return: Always 0 (Success)
 */
int main(void)
{
	int _s_digit = 48;

	while (_s_digit < 58)
	{
		putchar(_s_digit);
		_s_digit++;
	}
	putchar(10);
	return (0);
}
