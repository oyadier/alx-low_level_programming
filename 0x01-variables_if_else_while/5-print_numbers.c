#include <stdio.h>
/**
 * main - contain list of single digit numbers
 * Return: Always 0 (Success)
 */
int main(void)
{
	int _numbers = 48;

	while (_numbers < 58)
	{
		putchar(_number);
		_numbers++;
	}
	putchar(10);
	return (0);
}
