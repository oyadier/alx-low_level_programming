#include <stdio.h>
/**
 * main - printing of lowercase alphabet
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (int ch = 'a'; ch <= 'z'; a++)
	{
		putchar(ch);
	}
		putchar("\n");
	return (0);
}
